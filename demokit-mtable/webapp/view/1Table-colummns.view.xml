<mvc:View controllerName="com.sap.demokitmtable.controller.1Table-colummns"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m" xmlns:core="sap.ui.core">
    <Page id="page" title="Table-Columns">
        <Table id="idProductsTable" inset="true" items="{ path: '/Products', sorter: { path: 'ProductName'} }">
            <headerToolbar>
				<OverflowToolbar>
					<content>
						<Title text="Products" level="H2"/>
						<ToolbarSpacer />
						<ComboBox id="idPopinLayout"
							placeholder="Popin layout options"
							change="onPopinLayoutChanged">
							<items>
								<core:Item  text="Block"
									key="Block"/>
								<core:Item  text="Grid Large"
									key="GridLarge"/>
								<core:Item  text="Grid Small"
									key="GridSmall"/>
							</items>
						</ComboBox>
						<Label text="Sticky options:" />
						<CheckBox text="ColumnHeaders"
							select="onSelect"/>
						<CheckBox text="HeaderToolbar"
							select="onSelect"/>
						<CheckBox text="InfoToolbar"
							select="onSelect"/>
						<ToggleButton id="toggleInfoToolbar"
							text="Hide/Show InfoToolbar"
							press="onToggleInfoToolbar" />
					</content>
				</OverflowToolbar>
			</headerToolbar>
			<infoToolbar>
				<OverflowToolbar>
					<Label text="Wide range of available products"/>
				</OverflowToolbar>
			</infoToolbar>
			<columns>
				<Column
					width="12em">
					<Text text="Product" />
				</Column>
				<Column
					minScreenWidth="Tablet"
					demandPopin="true">
					<Text text="SupplierID" />
				</Column>
				<Column
					minScreenWidth="Desktop"
					demandPopin="true"
					hAlign="End">
					<Text text="QuantityPerUnit" />
				</Column>
				<Column
					minScreenWidth="Desktop"
					demandPopin="true"
					hAlign="Center">
					<Text text="Discontinued" />
				</Column>
				<Column
					hAlign="End">
					<Text text="UnitPrice" />
				</Column>
			</columns>
			<items>
				<ColumnListItem vAlign="Middle">
					<cells>
						<ObjectIdentifier
							title="{ProductName}"
							text="{ProductID}"/>
						<Text
							text="{SupplierID}" />
						<Text
							text="{QuantityPerUnit}" />
						<Text
							text="{Discontinued}" />
						<Text
							text="{UnitPrice}" />
					</cells>
				</ColumnListItem>
			</items>
        </Table>
		<footer>
			<OverflowToolbar >
				<ToolbarSpacer/>
				<Button text="Go to next page" press="onPressNext"></Button>
				<CheckBox text="test" select="onTestCheckbox"/>
				<Button id="testId" text ="Test Button" visible="false"/>
				<Button id="secondbtn" text ="Second Button" visible="false"/>
			</OverflowToolbar>
		</footer>
    </Page>
</mvc:View>